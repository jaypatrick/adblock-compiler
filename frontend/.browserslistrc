# Browserslist configuration for the Angular frontend.
# Angular CLI (esbuild) uses this to determine the minimum browser targets,
# which controls which syntax transforms and polyfills are emitted.
# Keeping targets modern avoids unnecessary polyfill overhead.
last 2 Chrome versions
last 2 Firefox versions
last 2 Safari versions
last 2 Edge versions
