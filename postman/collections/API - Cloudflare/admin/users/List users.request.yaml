$kind: http-request
description: >
  Lists all users in the system with optional pagination. Requires admin authentication.
url: {{baseUrl}}/admin/users
method: GET
headers:
  Accept: application/json
  X-Admin-Key: "{{adminKey}}"
body:
  type: text
  content: ""
examples: ./.resources/List users.resources/examples
order: 1000
