$kind: http-request
description: Resolves a configuration by merging defaults and applying transformations.
url: {{baseUrl}}/configuration/resolve
method: POST
headers:
  Content-Type: application/json
  Accept: application/json
  X-API-Key: "{{apiKey}}"
body:
  type: json
  content: |-
    {
      "configuration": {
        "name": "My Filter List",
        "sources": [
          {
            "source": "https://example.com/filters.txt",
            "transformations": ["RemoveComments"]
          }
        ],
        "transformations": ["Deduplicate"]
      }
    }
order: 3000
