$kind: http-request
description: Returns job history and queue depth over time
url: {{baseUrl}}/queue/history
method: GET
headers:
  Accept: application/json
  X-API-Key: "{{apiKey}}"
body:
  type: text
  content: ""
examples: ./.resources/Get queue job history.resources/examples
order: 1000
