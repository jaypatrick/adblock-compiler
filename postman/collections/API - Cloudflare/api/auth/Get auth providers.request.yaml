$kind: http-request
description: >
  Returns which authentication providers are currently active (email/password,
  GitHub, Google). Public endpoint — no authentication required.
url: {{baseUrl}}/api/auth/providers
method: GET
headers:
  Accept: application/json
body:
  type: text
  content: ""
examples: ./.resources/Get auth providers.resources/examples
order: 1000
